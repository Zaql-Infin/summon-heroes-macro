opencv-python>=4.9.0
numpy>=1.26.0
mss>=9.0.1
pyautogui>=0.9.54
pynput>=1.7.6
PyYAML>=6.0.1
pytesseract>=0.3.13
ultralytics>=8.4.0
anthropic>=1.4.0
python-dotenv>=1.2.0
ruamel.yaml>=0.19.0
pywebview>=6.0

# Windows only — the GUI's click-through overlay windows and window-title
# lookup use raw Win32 calls (ctypes.windll) directly, not these packages,
# but pywin32 is what pywebview's own Windows backend (WebView2) needs.
pywin32>=306; sys_platform == "win32"

# macOS only (2026-09-12) — Quartz for genuine relative mouse-motion events
# (see modules/input_sim.py; Roblox's camera-look ignores absolute cursor
# teleports the same way it does on Windows) and AppKit for the
# frontmost-app check hotkeys use to only fire while Roblox is focused.
# Also what pywebview's Cocoa backend needs for the control panel window.
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
